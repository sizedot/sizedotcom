  <table class="tabletop bordered striped centered responsive-table highlight" style="min-width:345px;">
    <thead id="showselect">
    <tr class="frow" style="border-bottom-width:2px;">
      <td class="fcol" colspan="7" style="line-height: 0px">
        <b>
          <% @showresu.each do |p| %>
              카테고리 분류 : <%= p.division %>
          <% end %>
        </b>
      </td>
    </tr>
    <tr class="frow" style="border-bottom-width:2px;">
      <td class="fcol" id="showme" colspan="7" style="line-height: 0px">
        <% @productsel.each do |p| %>
            <p class="btntext">
              <input type="radio" name="psel" value="<%= p.productname %>" id="<%= p.productname %>" />
              <label for="<%= p.productname %>"><%= p.productname %></label>
            </p>
        <% end %>
        <p class="btntext">
          <input type="radio" name="psel" value="average" id="average" />
          <label for="average">평균</label>
        </p>
      </td>
    </tr>
    </thead>
  </table>
  <button class="btn waves-effect waves-light" id="reply_sub" style="margin-top:-5px; float:right;" type="submit" name="action">확인
  </button>

  <script language="JavaScript">
//    //현재 페이지에서 카테고리를 선택하면 해당 카테고리 보여주는 폼이 나오도록하는 ajax
//    $(document).ready(function () {
//      $('#form1').hide();
//
//      $("body").on('click', '#reply_submit', function () {
//        var brand_info = $("input:radio[name='bsel2']:checked").val();
//        var category_info = $("input:radio[name='csel2']:checked").val();
//
//        if (brand_info == ""||brand_info==null) {
//          (alert("브랜드를 설정해주세요."));
//          return;
//        }
//        else if (category_info == ""||category_info==null){
//          alert("카테고리를 설정해주세요.");
//          return;
//        }
//        else{
//          $.ajax({
//            method: "GET",
//            url: "/datashow",
//            data: {csel2: category_info, bsel2: brand_info},
//            success: function(){
//              $('#form1').show();
//              var url = '/branddic'; //please insert the url of the your current page here, we are assuming the url is 'index.php'
//              $('#form1').load(url + ' #showtable'); //note: the space before #div1 is very important
//            }
//          })
//        }
//      });
//
//      $("body").on('click', '#reply_sub', function () {
//        var psel_info = $("input:radio[name='psel']:checked").val();
//
//        if (psel_info == ""||psel_info==null) {
//          (alert("제품을 선택해주세요."));
//          return;
//        }
//        else{
//          $.ajax({
//            method: "GET",
//            url: "/datashow2",
//            data: {psel: psel_info},
//            success: function(){
//              $('#form2').show();
//              var url = '/branddic'; //please insert the url of the your current page here, we are assuming the url is 'index.php'
//              $('#form2').load(url + ' #showcontent'); //note: the space before #div1 is very important
//            }
//          });
//        }
//      });
//
//    });
//
  </script>